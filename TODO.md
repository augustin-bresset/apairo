# TODO list

## Standard

### Keys

In `src/core/config/keys.yaml` :
- [ ] Fulfill the keys with their parameters

## Tests
### Unitary
- [ ] Tests for core object (abstract dataset, sampler, loader, ...)
- [ ] Tests for loader classes
- [ ] Tests for dataset classes (kind of integration tests of loader too)
- [ ] Tests for sampler
- [ ] Tests for `Torch.DataLoader`
